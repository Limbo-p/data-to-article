比较下面的原文和事件描述，判断它们是否为同一件事（包括同一件事的后续、回应或不同视角）。
判断依据：核心实体、关键数字、事件进展和正文事实；标题相似只作参考。
无法判断时输出 "match": "unknown"。

原文：
{article}

事件描述：
{event}

仅输出 JSON：
{
  "match": true 或 false 或 "unknown",
  "reason": "一句话理由"
}